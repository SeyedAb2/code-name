# بریف ساخت مرجع‌های آموزشی فارسی — نسخهٔ کامل برای ایجنت کد

> این فایل را در ریشهٔ ورک‌اسپیس بگذارید و به ایجنت بگویید: «این بریف را بخوان و طبق آن کار کن.»
> برای شروع هر پروژه، فقط نام آن پروژه را به ایجنت بدهید (مثلاً «پروژهٔ Docker را شروع کن، فصل ۱»).

---

## ۰) نقش تو

تو نویسنده و مهندس فرانت‌اند یک تیم مستندسازی فنی فارسی‌زبان هستی. خروجی کارت یک
مجموعه «مرجع آموزشی» است شبیه مستندات رسمی زبان‌های برنامه‌نویسی
(MDN، Docker Docs، PostgreSQL Manual) — نه یک وبلاگ‌پست، نه یک اسلاید، نه یک PDF.

هدف: خواننده‌ای که **هیچ چیز نمی‌داند** بتواند از فصل ۱ شروع کند و در پایان
پروژه، کار حرفه‌ای و عملیاتی انجام دهد.

**قانون مطلق دربارهٔ منابع:** حق نداری متن هیچ کتاب یا مستند تجاری را ترجمه یا
بازتولید کنی. همه چیز باید با ادبیات، ساختار و مثال‌های خودت نوشته شود. سرفصل‌ها
راهنمای پوشش موضوعی‌اند، نه دستور ترجمه.

---

## ۱) پروژه‌ها

هر موضوع یک پوشهٔ کاملاً مستقل است. بین پروژه‌ها هیچ وابستگی‌ای وجود ندارد.

```
/docs-fa
  /01-docker            آموزش داکر — کاربردی، از صفر تا استقرار واقعی
  /02-nginx             آموزش Nginx — از reverse proxy تا tuning
  /03-linux-network     لینوکس کاربردی برای شبکه
  /04-devops            DevOps — CI/CD، IaC، مانیتورینگ
  /05-sql               SQL و SQL Server — از SELECT تا performance tuning
  /06-csharp            زبان ‎C#‎ (اختیاری، بعداً)
  /07-aspnet-core       ASP.NET Core (اختیاری، بعداً)
```

ساختار داخل **هر** پروژه:

```
/01-docker
  index.html              صفحهٔ خانه: معرفی، نقشهٔ راه، فهرست همهٔ فصل‌ها، نوار پیشرفت
  /ch/01-intro.html       هر فصل یک فایل HTML مستقل و قابل باز شدن با دابل‌کلیک
  /ch/02-images.html
  ...
  /assets/theme.css       تمام استایل‌ها؛ مشترک بین همهٔ فصل‌ها
  /assets/app.js          دارک‌مود، دکمهٔ کپی، فهرست شناور، پیشرفت مطالعه، جستجو
  /assets/fonts/          Vazirmatn + JetBrains Mono به‌صورت لوکال (woff2)
  /lab/                   فایل‌های تمرین: Dockerfile، compose.yml، اسکریپت‌ها
  README.md               چه ساخته شد، چه چیزی باقی مانده
```

**هر فایل HTML باید بدون سرور و بدون اینترنت باز شود.** لینک فونت از گوگل ممنوع —
فونت‌ها لوکال. هیچ CDN، هیچ build step، هیچ فریم‌ورک. فقط HTML + CSS + JS خام.

---

## ۲) سیستم طراحی — دقیقاً همین، بدون بازطراحی

فایل `deploy-guide.html` نمونهٔ مرجع است. CSS آن را استخراج کن و در
`assets/theme.css` بگذار. توکن‌ها را عوض نکن.

```css
:root{
  --canvas:#F4F7FC; --surface:#FFFFFF; --surface-sunk:#EDF1F8; --surface-code:#0E1524;
  --ink:#0F1729; --ink-soft:#55617C; --ink-faint:#7A87A3;
  --line:rgba(16,24,40,.10); --line-strong:rgba(16,24,40,.18);
  --blue:#4F6BF5; --violet:#8B5CF6;
  --brand-grad:linear-gradient(120deg,#4F6BF5 0%,#8B5CF6 100%);
  --safe:#15A34A; --warn:#B45309; --danger:#DC2626;
  --fa:'Vazirmatn',system-ui,sans-serif;
  --mono:'JetBrains Mono',ui-monospace,Consolas,monospace;
  --measure:68ch;
}
```

قواعد ثابت:

- `<html lang="fa" dir="rtl">` — متن فارسی راست‌چین.
- بدنه: ‎16.5px‎ / ‎line-height: 1.85‎ / عرض متن حداکثر `68ch`.
- دارک‌مود از دو راه: `prefers-color-scheme` و صفت `data-theme="dark"` روی `:root`.
  انتخاب کاربر در `localStorage` بماند.
- شبکهٔ صفحه: ستون فهرست چسبان ‎232px‎ + ستون محتوا، در `min-width: 1040px`.
  زیر آن، فهرست به یک دکمهٔ کشویی تبدیل شود.
- `prefers-reduced-motion` رعایت شود. فوکوس کیبورد دیده شود.
- هیچ انیمیشن تزئینی. حرکت فقط در پاسخ به کلیک کاربر (باز شدن پاسخ تمرین، کپی).

---

## ۳) قرارداد کامپوننت‌ها — فقط از همین‌ها استفاده کن

کامپوننت جدید اختراع نکن. اگر واقعاً لازم شد، اول در README ثبتش کن.

| کامپوننت | کلاس | کِی استفاده شود |
|---|---|---|
| سربرگ فصل | `.masthead` + `.eyebrow` + `.facts` | ابتدای هر فصل؛ `.fact` = عدد یا واقعیت کلیدی |
| فهرست کناری | `.toc` | لینک به همهٔ `<section id>`های همان فصل |
| جملهٔ راهنما | `p.lede` | اولین پاراگراف هر بخش، درشت‌تر و کم‌رنگ‌تر |
| کارت مفهوم | `.cards > .card` | ۲ تا ۴ مفهوم هم‌تراز؛ آیکون SVG ‎16px‎ inline |
| ترمینال | `.term` | هر دستور شل — با نوار سه‌نقطهٔ رنگی و برچسب |
| هشدار | `.note` `.note.safe` `.note.warn` `.note.danger` | نکته / امن / احتیاط / خطرناک |
| نمودار | `figure > .diagram > svg` + `figcaption` | معماری، جریان داده، چرخهٔ عمر |
| جدول | `.table-wrap > table` | مقایسه، مرجع سوئیچ‌ها، ماتریس تصمیم |
| مراحل شماره‌دار | `ol.ranked` | **فقط** وقتی محتوا واقعاً ترتیبی است |

### کامپوننت‌های جدیدی که باید بسازی

**تمرین** — این ستون فقرات کل پروژه است:

```html
<div class="ex" id="ex-3-2">
  <div class="ex-h">
    <span class="ex-n">تمرین ۳٫۲</span>
    <span class="ex-lvl lvl-2">متوسط</span>
    <span class="ex-time">۱۵ دقیقه</span>
  </div>
  <p>صورت مسئله — دقیق، با ورودی و خروجی مشخص.</p>
  <p class="ex-goal">معیار موفقیت: خروجی <code>curl -I localhost</code> باید ‎200‎ برگرداند.</p>
  <details class="ex-hint"><summary>راهنمایی</summary><p>…</p></details>
  <details class="ex-sol"><summary>پاسخ و توضیح</summary>
    <!-- کد کامل + چرا این‌طور، نه فقط چه چیزی -->
  </details>
</div>
```

سطح‌ها: `lvl-1` مبتدی (سبز)، `lvl-2` متوسط (آبی)، `lvl-3` پیشرفته (بنفش).

**بلوک کد فایل** (متمایز از ترمینال — این محتوای یک فایل است):

```html
<div class="code"><div class="code-bar"><span class="code-path">nginx/site.conf</span>
<button class="copy">کپی</button></div><pre dir="ltr">…</pre></div>
```

**مرجع سریع** `.cheat` — انتهای هر فصل، جدول فشردهٔ دستورها.
**دام‌ها** `.pitfalls` — فهرست «خطای رایج ← علت ← درمان».

---

## ۴) قواعد نوشتن

- فارسی مهندسی، روشن و بی‌تعارف. جملهٔ کوتاه. فعل فعال.
- اصطلاح فنی را **ترجمه نکن**؛ داخل `<code>` انگلیسی بنویس:
  container، volume، reverse proxy، index، deadlock.
  یک‌بار در اولین برخورد، معادل فارسی را در پرانتز بیاور.
- ممنوع: «همان‌طور که می‌دانید»، «در دنیای امروز»، «بسیار مهم است که».
- هر مفهوم دقیقاً این چهار سؤال را پاسخ دهد:
  1. **چیست** — تعریف در دو جمله.
  2. **چرا** — چه دردی را درمان می‌کند؟ (اول درد، بعد ابزار)
  3. **چطور** — کد واقعی و اجراشدنی.
  4. **کجا خراب می‌شود** — دام‌ها، خطاهای واقعی، پیام خطای دقیق.
- هیچ ادعای عددی بدون منبع. اگر مطمئن نیستی، ننویس.
- عدد فارسی در متن (۱۲ کانتینر)، عدد انگلیسی در کد و مسیر (`port 8080`).

## ۵) قواعد کد

- هر بلوک کد `dir="ltr"` باشد، وگرنه در RTL به‌هم می‌ریزد.
- کامنت داخل کد **فارسی** — این تفاوت اصلی با مستندات انگلیسی است.
- هر دستور غیربدیهی، خروجی واقعی‌اش هم نشان داده شود (با کلاس `.s`).
- رنگ‌بندی ترمینال با span دستی: `.k` دستور، `.f` سوئیچ، `.c` کامنت،
  `.s` خروجی/رشته، `.w` تأکید. هیچ کتابخانهٔ highlight اضافه نکن.
- دستور مخرب حتماً داخل `.note.danger` با توضیح اینکه دقیقاً چه چیزی از بین می‌رود.
- کد باید کپی‌پیست‌شدنی باشد: بدون `$` ابتدای خط، بدون `...` وسط.

---

## ۶) سهمیهٔ محتوای هر فصل

| مورد | حداقل |
|---|---|
| بخش (`<section id>`) | ۵ |
| کلمهٔ توضیح | ۲۵۰۰ |
| بلوک ترمینال/کد | ۱۲ |
| نمودار SVG | ۱ |
| جدول | ۱ |
| تمرین | ۸ (‎۳ مبتدی، ۳ متوسط، ۲ پیشرفته‎) |
| دام رایج | ۴ |
| پروژهٔ کوچک پایان فصل | ۱ |

هر پروژه در پایان یک **پروژهٔ نهایی (Capstone)** دارد که همهٔ فصل‌ها را به هم وصل می‌کند.

---

## ۷) سرفصل‌ها

### ۰۱ — Docker
۱ مسئله‌ای که داکر حل می‌کند؛ image / container / volume ·
۲ نصب روی ویندوز، لینوکس، WSL2 · ۳ اجرا، لاگ، exec، پورت ·
۴ نوشتن Dockerfile؛ لایه‌ها و کش · ۵ multi-stage build و کوچک‌کردن ایمیج ·
۶ داده: volume، bind mount، پشتیبان‌گیری · ۷ شبکه: bridge، DNS داخلی، publish ·
۸ Docker Compose · ۹ متغیر محیطی و secret · ۱۰ healthcheck، restart policy، لاگ ·
۱۱ registry و push · ۱۲ عیب‌یابی: exit code، OOM، پر شدن دیسک ·
۱۳ امنیت: non-root، read-only، محدودیت منابع · ۱۴ استقرار روی VPS ·
**Capstone:** استقرار یک اپ سه‌سرویسه با پایگاه‌داده، پشتیبان‌گیری خودکار و healthcheck.

### ۰۲ — Nginx
۱ نصب و ساختار فایل‌ها · ۲ دستور زبان کانفیگ؛ context‌ها ·
۳ server block و virtual host · ۴ منطق `location` و اولویت‌ها ·
۵ فایل استاتیک، کش مرورگر، فشرده‌سازی · ۶ reverse proxy و هدرها ·
۷ load balancing و upstream · ۸ HTTPS، Let's Encrypt، تمدید خودکار ·
۹ rate limiting و محافظت · ۱۰ لاگ و آنالیز · ۱۱ WebSocket و آپلود حجیم ·
۱۲ tuning: worker، buffer، timeout · ۱۳ عیب‌یابی ۴۰۲/۵۰۲/۵۰۴ ·
**Capstone:** یک gateway کامل جلوی سه سرویس با SSL، کش، و محدودیت نرخ.

### ۰۳ — Linux برای شبکه
۱ شل و فایل‌سیستم · ۲ کاربر، گروه، مجوز · ۳ پردازه و سرویس (systemd) ·
۴ بسته و ریپازیتوری · ۵ ابزار متن: grep، awk، sed · ۶ اسکریپت‌نویسی bash ·
۷ مدل شبکه در عمل: `ip`، route، DNS · ۸ عیب‌یابی: ping، traceroute، `ss`، tcpdump ·
۹ فایروال: nftables / ufw · ۱۰ SSH، کلید، tunnel، `~/.ssh/config` ·
۱۱ لاگ و journald · ۱۲ مانیتورینگ منابع و دیسک · ۱۳ cron و زمان‌بندی ·
۱۴ سخت‌سازی سرور · **Capstone:** آماده‌سازی کامل یک VPS تازه برای production.

### ۰۴ — DevOps
۱ DevOps چیست و چه چیزی نیست · ۲ Git در عمل: branch، merge، rebase، استراتژی ·
۳ CI: تست خودکار، lint، build (GitHub Actions) · ۴ CD و محیط‌ها ·
۵ ساخت artifact و نسخه‌گذاری · ۶ IaC با Terraform · ۷ Ansible ·
۸ مانیتورینگ: Prometheus + Grafana · ۹ لاگ متمرکز · ۱۰ alert و on-call ·
۱۱ استراتژی انتشار: blue-green، canary، rollback · ۱۲ مدیریت secret ·
۱۳ backup و disaster recovery · ۱۴ postmortem ·
**Capstone:** خط لولهٔ کامل از commit تا production با rollback خودکار.

### ۰۵ — SQL و SQL Server
۱ مدل رابطه‌ای؛ نصب SQL Server و SSMS/Azure Data Studio ·
۲ `SELECT`، فیلتر، مرتب‌سازی · ۳ JOIN‌ها با نمودار · ۴ تجمیع و `GROUP BY` ·
۵ زیرکوئری و CTE · ۶ window function · ۷ `INSERT`/`UPDATE`/`DELETE`/`MERGE` ·
۸ طراحی جدول، نوع داده، کلید، constraint · ۹ نرمال‌سازی و کِی نقضش کنیم ·
۱۰ index: clustered، nonclustered، covering · ۱۱ خواندن execution plan ·
۱۲ تراکنش، سطح ایزوله، deadlock · ۱۳ stored procedure، function، trigger ·
۱۴ T-SQL: متغیر، حلقه، خطا · ۱۵ backup/restore و نگهداری ·
۱۶ امنیت و مجوز · ۱۷ بهینه‌سازی کوئری کند ·
**Capstone:** طراحی و بهینه‌سازی پایگاه‌دادهٔ یک سیستم فروش با دادهٔ حجیم.

### ۰۶ / ۰۷ — C# و ASP.NET Core
فعلاً شروع نکن. وقتی پنج پروژهٔ بالا تمام شد، سرفصلش را جداگانه می‌دهم.

---

## ۸) روش کار — مهم

**یک فصل در هر نوبت. نه بیشتر.** فصل طولانی است؛ اگر چند فصل را همزمان بنویسی
کیفیت می‌افتد و خروجی ناقص می‌ماند.

ترتیب کار برای هر پروژه:

1. اول `assets/theme.css` و `assets/app.js` و `index.html` را بساز.
2. یک فصل نمونه (فصل ۱) کامل بنویس و **متوقف شو** تا تأیید بگیری.
3. بعد از تأیید، فصل به فصل جلو برو.
4. بعد از هر فصل: لینک آن را در `index.html` فعال کن و در README علامت بزن.

قبل از تحویل هر فصل این چک را انجام بده:

- [ ] فایل با دابل‌کلیک و بدون اینترنت درست باز می‌شود
- [ ] در دارک‌مود و لایت‌مود هر دو خوانا است
- [ ] در عرض ‎375px‎ به‌هم نمی‌ریزد
- [ ] هیچ متن انگلیسی راست‌چین و هیچ کد چپ‌چین‌نشده نمانده
- [ ] همهٔ دستورها را ذهنی اجرا کردی و درست‌اند
- [ ] همهٔ `id`های فهرست کناری وجود دارند
- [ ] دکمهٔ کپی روی همهٔ بلوک‌های کد کار می‌کند
- [ ] سهمیهٔ بخش ۶ رعایت شده
- [ ] هیچ بخشی TODO یا نیمه‌کاره نمانده

---

## ۹) ممنوعیت‌ها

- ❌ فریم‌ورک، CDN، npm، build step
- ❌ `localStorage` برای چیزی جز تم و پیشرفت مطالعه
- ❌ کد ساختگی که اجرا نمی‌شود
- ❌ «این را می‌توانید خودتان امتحان کنید» به‌جای نوشتن پاسخ تمرین
- ❌ محتوای تکراری بین فصل‌ها (به‌جایش لینک بده)
- ❌ ترجمه یا بازتولید متن کتاب‌ها
- ❌ خلاصه‌نویسی برای صرفه‌جویی در طول خروجی

---

## ۱۰) اولین کار

پروژهٔ `01-docker` را شروع کن:
`theme.css` + `app.js` + `index.html` + فصل ۱ کامل — بعد متوقف شو و نشانم بده.