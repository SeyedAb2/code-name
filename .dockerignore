# هرچه در ایمیج لازم نیست، وارد context هم نشود.
# context کوچک‌تر = بیلد سریع‌تر (فصل ۴ داکر)

.git
.gitignore
.claude/

# نصب و بیلد داخل خود ایمیج انجام می‌شود
node_modules
.next
out

# ابزارهای نویسنده؛ دادهٔ تولیدی‌شان (src/lib/courses.data.ts) در مخزن هست
tools
data

# مستندات مخزن، نه سایت
*.md
Dockerfile
.dockerignore
compose.yml
deploy

# موقت
*.log
*.tsbuildinfo
