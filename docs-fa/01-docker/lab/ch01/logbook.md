# دفترچهٔ فصل ۱ — اولین کانتینر

> این فایل را خودت پر کن. هدف این است که شش ماه بعد بتوانی بدون فکر کردن
> همین کار را تکرار کنی، و بتوانی به کس دیگری هم بدهی.

---

## ۱) محیط

| مورد | مقدار |
|---|---|
| سیستم‌عامل | |
| خروجی `docker --version` | |
| خروجی `docker info --format '{{.ServerVersion}}'` | |

---

## ۲) دستورهایی که زدم

هر دستور را با خروجی یک‌خطی‌اش بنویس.

```bash
# مثال:
# docker run -d --name ch01-site -p 8080:80 nginx:1.27-alpine
# → 4f2a9c1b7e3d... (شناسهٔ کانتینر)
```

| # | دستور | نتیجه در یک خط |
|---|---|---|
| ۱ | | |
| ۲ | | |
| ۳ | | |
| ۴ | | |
| ۵ | | |

---

## ۳) لاگ

کد وضعیتی که برای `/nothing` در لاگ ثبت شد:

```
```

خط `[error]` که Nginx نوشت:

```
```

---

## ۴) آزمون مقاومت

**بعد از `docker stop` و بعد `docker start` — صفحهٔ من هنوز بود؟**

پاسخ:

**چرا؟**

پاسخ:

---

**بعد از `docker rm -f` و ساخت دوبارهٔ کانتینر — صفحهٔ من هنوز بود؟**

پاسخ:

**چرا؟**

پاسخ:

---

## ۵) چه چیزی کم دارم

اگر می‌خواستم این سرویس روی یک سرور واقعی همیشه بالا بماند، این‌ها را ندارم:

1.
2.
3.

---

## ۶) تمیزکاری

دستوری که در پایان زدم تا هیچ چیزی روی سیستمم باقی نماند:

```bash
```
