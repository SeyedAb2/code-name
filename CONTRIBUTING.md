<div align="right">

# راهنمای مشارکت در کدنامه

خوش آمدی. این سند دقیقاً می‌گوید چطور یک **فصل** یا یک **دورهٔ کامل** اضافه کنی.
هیچ دانش قبلی از این پروژه لازم نیست؛ فقط HTML مقدماتی.

</div>

---

## فهرست

1. [قبل از هر کاری](#۰-قبل-از-هر-کاری)
2. [راه‌اندازی](#۱-راه-اندازی)
3. [معماری پروژه در سه دقیقه](#۲-معماری-پروژه-در-سه-دقیقه)
4. [اضافه کردن یک فصل](#۳-اضافه-کردن-یک-فصل)
5. [اضافه کردن یک دورهٔ کامل](#۴-اضافه-کردن-یک-دورهٔ-کامل)
6. [قواعد نوشتن](#۵-قواعد-نوشتن)
7. [کامپوننت‌های آماده](#۶-کامپوننت-های-آماده)
8. [چک‌لیست قبل از ارسال](#۷-چک-لیست-قبل-از-ارسال)
9. [ارسال Pull Request](#۸-ارسال-pull-request)

---

## ۰. قبل از هر کاری

**قانون مطلق:** حق نداری متن هیچ کتاب یا مستند تجاری را ترجمه یا بازتولید کنی.
همه‌چیز باید با ادبیات، ساختار و مثال‌های خودت نوشته شود. سرفصل‌ها راهنمای
پوشش موضوعی‌اند، نه دستور ترجمه. مشارکتی که ترجمهٔ مستقیم باشد پذیرفته نمی‌شود.

**قانون دوم:** هر دستوری که می‌نویسی را باید واقعاً اجرا کرده باشی.
خروجی‌ای که نشان می‌دهی باید خروجی واقعی باشد، نه حدس.

---

## ۱. راه‌اندازی

```bash
git clone <آدرس-مخزن>
cd codenameh
```

برای **خواندن**، هیچ ابزاری لازم نیست: `docs-fa/index.html` را باز کن.

برای **نوشتن**، فقط اگر می‌خواهی صفحه‌ها را دوباره بسازی به Node نیاز داری
(نسخهٔ ۱۸ به بالا):

```bash
node tools/build-pages.js
```

این دستور صفحهٔ اصلی و خانهٔ همهٔ مسیرها را از روی مانیفست از نو می‌سازد.
هیچ وابستگی‌ای نصب نمی‌شود — `node_modules` وجود ندارد.

---

## ۲. معماری پروژه در سه دقیقه

سه چیز را بفهمی، همه‌چیز را فهمیده‌ای.

### الف) `docs-fa/assets/courses.js` — تنها منبع حقیقت

فهرست همهٔ دوره‌ها و فصل‌ها اینجاست. صفحهٔ اصلی، خانهٔ هر مسیر، ستون کناری،
دکمهٔ بعدی/قبلی، جستجو و محاسبهٔ پیشرفت — همه از همین می‌خوانند.

هر فصل یک آرایهٔ فشرده است:

```js
["04", "04-dockerfile.html", 0, 12, 60,
 "نوشتن Dockerfile؛ لایه‌ها و کش",
 "هر دستور یک لایه است. ترتیب دستورها یعنی تفاوت بیلد ۲ ثانیه‌ای و ۲ دقیقه‌ای.",
 "Writing a Dockerfile; layers and cache",
 "Every instruction is a layer. Order is the difference between a 2-second and a 2-minute build.",
 "dockerfile from run copy cmd entrypoint layer cache"]
```

| موقعیت | معنی |
|---|---|
| `[0]` | شمارهٔ فصل، دو رقمی، رشته‌ای — `"04"` |
| `[1]` | نام فایل HTML |
| `[2]` | آماده است؟ `0` = هنوز نه، `1` = نوشته شده |
| `[3]` | تعداد تمرین‌ها |
| `[4]` | زمان مطالعه به دقیقه |
| `[5]` `[6]` | عنوان و توضیح **فارسی** |
| `[7]` `[8]` | عنوان و توضیح **انگلیسی** |
| `[9]` | کلیدواژه‌های جستجو (فارسی و انگلیسی، با فاصله) |
| `[10]` | فقط برای پروژهٔ نهایی: `1` ساده، `2` متوسط، `3` پیچیده |

### ب) `docs-fa/assets/theme.css` — کل سیستم طراحی

تنها فایل استایل. رنگ‌ها همه از توکن‌های `:root` می‌آیند و در دارک‌مود
خودکار عوض می‌شوند. **هیچ رنگی را مستقیم ننویس** — همیشه `var(--…)`.

### ج) `docs-fa/assets/app.js` — رفتار مشترک

تم، سوییچ زبان، دکمهٔ کپی، ساخت فهرست کناری از روی خود صفحه، ردیابی
بخش فعال، پیشرفت مطالعه و تمرین، جستجو، و کشوی موبایل.

**تو معمولاً به این فایل دست نمی‌زنی.** فقط کلاس‌های درست را در HTML بگذار.

---

## ۳. اضافه کردن یک فصل

فرض کنیم می‌خواهی فصل ۴ داکر را بنویسی.

### گام ۱ — فایل را از روی الگو بساز

```bash
cp docs-fa/01-docker/ch/01-intro.html docs-fa/01-docker/ch/04-dockerfile.html
```

فصل ۱ داکر **الگوی مرجع** است. هر کامپوننتی که لازم داری، آنجا نمونه‌اش هست.

### گام ۲ — سرِ فایل را درست کن

```html
<title>فصل ۴ — نوشتن Dockerfile | داکر | کدنامه</title>
<meta name="description" content="یک جملهٔ دقیق دربارهٔ محتوای این فصل.">
```

و مهم‌تر از همه، صفت‌های `<body>`:

```html
<body data-page="chapter" data-course="01-docker" data-chapter="04">
```

| صفت | مقدار |
|---|---|
| `data-page` | همیشه `chapter` برای فصل |
| `data-course` | دقیقاً همان `id` در `courses.js` |
| `data-chapter` | دقیقاً همان `[0]` فصل — `"04"` |

اگر این سه را اشتباه بزنی، ستون کناری و نوار پیشرفت کار نمی‌کنند.

### گام ۳ — محتوا را بنویس

هر بخش یک `<section id="...">` با یک `<h2>` است. فهرست کناری **خودکار** از
همین‌ها ساخته می‌شود؛ لازم نیست دستی چیزی به آن اضافه کنی.

```html
<section id="layers">
  <h2><span lang="fa">لایه‌ها</span><span lang="en">Layers</span></h2>
  <p class="lede" lang="fa">جملهٔ راهنمای این بخش.</p>
  <p class="lede" lang="en">The lede for this section.</p>
  …
</section>
```

### گام ۴ — دوزبانه بنویس

قاعده یکی است: **هر متنی که ترجمه دارد، دو بار می‌آید — یکی با `lang="fa"`
و یکی با `lang="en"`.** CSS خودش آن‌که لازم نیست را پنهان می‌کند.

```html
<p lang="fa">متن فارسی.</p>
<p lang="en">The English text.</p>
```

> ⚠️ **صفت `lang` را فقط برای همین کار استفاده کن.** اگر یک کلمهٔ انگلیسی
> وسط متن فارسی داری، از `<code>` یا `<span class="en">` استفاده کن، نه
> `lang="en"` — وگرنه در حالت فارسی ناپدید می‌شود.

بلوک‌های کد، نمودارها و جدول‌ها معمولاً صفت `lang` نمی‌گیرند و در هر دو
زبان دیده می‌شوند.

### گام ۵ — سهمیهٔ محتوا را رعایت کن

| مورد | حداقل |
|---|---|
| بخش (`<section id>`) | ۵ |
| کلمهٔ توضیح فارسی | ۲۵۰۰ |
| بلوک ترمینال یا کد | ۱۲ |
| نمودار SVG | ۱ |
| جدول | ۱ |
| تمرین | ۱۲ — ۴ مبتدی، ۵ متوسط، ۳ پیشرفته |
| دام رایج | ۴ |
| پروژهٔ کوچک پایان فصل | ۱ |

### گام ۶ — فصل را «آماده» اعلام کن

در `courses.js` مقدار `[2]` را از `0` به `1` تغییر بده:

```js
["04","04-dockerfile.html",1,12,60, …]
//                         ↑ اینجا
```

### گام ۷ — بساز و ببین

```bash
node tools/build-pages.js
```

فایل را در مرورگر باز کن و [چک‌لیست](#۷-چک-لیست-قبل-از-ارسال) را برو.

---

## ۴. اضافه کردن یک دورهٔ کامل

### گام ۱ — به `courses.js` اضافه کن

در انتهای یکی از بلوک‌های `C.push({...})` یک ورودی تازه بگذار:

```js
C.push({
  id:"23-rust", dir:"23-rust", accent:"#CE422B", cat:"backend",
  ico:'<path d="…"/>',            // SVG داخل viewBox 24×24، بدون تگ <svg>
  fa:{ name:"زبان Rust",
       desc:"یک جمله برای کارت صفحهٔ اصلی.",
       intro:"دو تا سه جمله برای سربرگ خانهٔ مسیر." },
  en:{ name:"Rust", desc:"…", intro:"…" },
  ch:[
    ["01","01-intro.html",0,12,50,"عنوان فارسی","توضیح فارسی",
     "English title","English description","keywords کلیدواژه"],
    // … بقیهٔ فصل‌ها
    ["15","15-cap1.html",0,5,60,"پروژهٔ ۱ — …","…","Project 1 — …","…","capstone",1],
    ["16","16-cap2.html",0,7,120,"پروژهٔ ۲ — …","…","Project 2 — …","…","capstone",2],
    ["17","17-cap3.html",0,9,180,"پروژهٔ ۳ — …","…","Project 3 — …","…","capstone",3]
  ]
});
```

### مقدار `cat` — دسته‌بندی صفحهٔ اصلی

| مقدار | دسته |
|---|---|
| `infra` | زیرساخت و عملیات |
| `arch` | معماری و مهندسی |
| `backend` | بک‌اند |
| `frontend` | فرانت‌اند و موبایل |
| `data` | داده و بی‌درنگ |
| `career` | مهارت و مسیر شغلی |

اگر دستهٔ تازه‌ای لازم داری، آن را در `tools/build-pages.js` به آرایهٔ
`CATS` اضافه کن.

### گام ۲ — ساختار پوشه

```bash
mkdir -p docs-fa/23-rust/ch docs-fa/23-rust/lab
```

### گام ۳ — بساز

```bash
node tools/build-pages.js
```

خانهٔ مسیر، کارت صفحهٔ اصلی، آمار، جستجو و دکمهٔ «شروع» **همه خودکار**
ساخته می‌شوند. هیچ HTMLای دست‌نویس نکن.

### قواعد طراحی سرفصل

- **هر مسیر با «چه دردی را درمان می‌کند» شروع می‌شود**، نه با نصب.
- **ترتیب فصل‌ها باید معنا داشته باشد** — هر فصل بر قبلی سوار شود.
- **سه پروژهٔ نهایی**، با سطح `1`، `2` و `3`.
- **محتوای تکراری بین مسیرها ممنوع** — به‌جایش لینک بده.

---

## ۵. قواعد نوشتن

**زبان**

- فارسی مهندسی، روشن و بی‌تعارف. جملهٔ کوتاه. فعل فعال.
- اصطلاح فنی را ترجمه نکن؛ داخل `<code>` انگلیسی بنویس: `container`، `index`،
  `deadlock`. یک‌بار در اولین برخورد، معادل فارسی را در پرانتز بیاور.
- **ممنوع:** «همان‌طور که می‌دانید»، «در دنیای امروز»، «بسیار مهم است که».
- عدد فارسی در متن (۱۲ کانتینر)، عدد انگلیسی در کد و مسیر (`port 8080`).

**کد**

- هر بلوک کد `dir="ltr"` باشد، وگرنه در RTL به‌هم می‌ریزد.
- کامنت داخل کد **فارسی**.
- بدون `$` ابتدای خط، بدون `...` وسط. باید کپی‌پیست‌شدنی باشد.
- رنگ‌بندی دستی با `<span>`: `.k` دستور · `.f` سوئیچ · `.c` کامنت ·
  `.s` خروجی · `.w` تأکید/خطا · `.p` مسیر یا مقدار · `.d` پرامپت (کپی نمی‌شود).
- **دستور مخرب حتماً داخل `.note.danger`** با توضیح اینکه دقیقاً چه چیزی از بین می‌رود.

---

## ۶. کامپوننت‌های آماده

کامپوننت جدید اختراع نکن. اگر واقعاً لازم شد، اول اینجا ثبتش کن.

| کامپوننت | کلاس |
|---|---|
| سربرگ فصل | `.masthead` + `.eyebrow` + `.facts > .fact` |
| جملهٔ راهنما | `p.lede` |
| کارت مفهوم | `.cards > .card` (`.v` بنفش، `.g` سبز، `.w` نارنجی) |
| ترمینال | `.term > .term-bar + pre` |
| محتوای فایل | `.code > .code-bar + pre` |
| هشدار | `.note` · `.note.safe` · `.note.warn` · `.note.danger` |
| نمودار | `figure > .diagram > svg` + `figcaption` |
| جدول | `.table-wrap > table` |
| مراحل ترتیبی | `ol.ranked` |
| تمرین | `.ex` (پایین) |
| مرجع سریع | `.cheat` |
| دام‌ها | `.pitfalls > .pit` |
| پروژه | `.capsule` |

### تمرین — ستون فقرات هر فصل

```html
<div class="ex" id="ex-4-2">
  <div class="ex-h">
    <span class="ex-n"><span lang="fa">تمرین ۴٫۲</span><span lang="en">Exercise 4.2</span></span>
    <span class="ex-lvl lvl-2"><span lang="fa">متوسط</span><span lang="en">Intermediate</span></span>
    <span class="ex-time"><span lang="fa">۱۵ دقیقه</span><span lang="en">15 min</span></span>
  </div>
  <p lang="fa">صورت مسئله — دقیق، با ورودی و خروجی مشخص.</p>
  <p lang="en">The problem — precise, with defined input and output.</p>
  <p class="ex-goal" lang="fa">معیار موفقیت: خروجی <code>curl -I localhost</code> باید ۲۰۰ برگرداند.</p>
  <p class="ex-goal" lang="en">Success criterion: <code>curl -I localhost</code> must return 200.</p>

  <details class="ex-hint">
    <summary><span lang="fa">راهنمایی</span><span lang="en">Hint</span></summary>
    <p lang="fa">…</p><p lang="en">…</p>
  </details>

  <details class="ex-sol">
    <summary><span lang="fa">پاسخ و توضیح</span><span lang="en">Solution</span></summary>
    <!-- کد کامل + توضیح «چرا این‌طور»، نه فقط «چه چیزی» -->
  </details>
</div>
```

- سطح‌ها: `lvl-1` مبتدی (سبز) · `lvl-2` متوسط (آبی) · `lvl-3` پیشرفته (بنفش)
- `id` هر تمرین باید **یکتا** باشد — از الگوی `ex-<فصل>-<شماره>` استفاده کن.
  نوار پیشرفت با همین `id` تمرین‌های حل‌شده را می‌شمارد.
- تیک «انجام شد» را خودکار `app.js` اضافه می‌کند؛ دستی ننویسش.

### نمودار

رنگ‌ها را مستقیم ننویس؛ از کلاس‌های آماده استفاده کن تا در دارک‌مود هم درست بماند:

`dg-fill` `dg-fill-b` `dg-fill-v` `dg-fill-g` `dg-fill-w` ·
`dg-stroke` `dg-stroke-b` `dg-stroke-v` `dg-stroke-g` `dg-stroke-w` ·
`dg-t` (تیتر) `dg-ts` (توضیح) `dg-tm` (مونو) `dg-tb` (آبی مونو) · `dg-arrow`

برچسب‌های دوزبانه در SVG هم با `lang` کار می‌کنند:

```html
<text class="dg-ts" x="100" y="20" text-anchor="middle" lang="fa">سیستم‌عامل میزبان</text>
<text class="dg-ts" x="100" y="20" text-anchor="middle" lang="en">Host operating system</text>
```

---

## ۷. چک‌لیست قبل از ارسال

- [ ] فایل با **دابل‌کلیک** و **بدون اینترنت** درست باز می‌شود
- [ ] در **دارک‌مود و لایت‌مود** هر دو خوانا است
- [ ] در عرض **۳۷۵px** به‌هم نمی‌ریزد و کشوی فهرست درست باز و بسته می‌شود
- [ ] سوییچ **FA/EN** همهٔ متن‌ها را عوض می‌کند و چیزی جا نمی‌ماند
- [ ] هیچ متن انگلیسی راست‌چین و هیچ بلوک کد بدون `dir="ltr"` نمانده
- [ ] **همهٔ دستورها را واقعاً اجرا کرده‌ای** و خروجی‌ها واقعی‌اند
- [ ] دکمهٔ **کپی** روی همهٔ بلوک‌های کد کار می‌کند
- [ ] هر `<section>` یک `id` یکتا دارد و در فهرست کناری دیده می‌شود
- [ ] هر `.ex` یک `id` یکتا دارد
- [ ] **سهمیهٔ محتوا** رعایت شده
- [ ] **هر تمرین پاسخ کامل دارد** — «خودتان امتحان کنید» وجود ندارد
- [ ] هیچ بخشی `TODO` یا نیمه‌کاره نمانده
- [ ] در `courses.js` مقدار `ready` روی `1` است
- [ ] `node tools/build-pages.js` اجرا شده
- [ ] در کنسول مرورگر خطایی نیست

---

## ۸. ارسال Pull Request

```bash
git checkout -b chapter/docker-04-dockerfile
git add .
git commit -m "افزودن فصل ۴ داکر: نوشتن Dockerfile"
git push origin chapter/docker-04-dockerfile
```

**نام شاخه:**

| کار | الگو |
|---|---|
| فصل تازه | `chapter/<مسیر>-<شماره>-<موضوع>` |
| دورهٔ تازه | `track/<نام>` |
| تصحیح | `fix/<توضیح-کوتاه>` |
| طراحی و رابط | `ui/<توضیح-کوتاه>` |

**در توضیح PR بنویس:**

- کدام فصل یا دوره
- روی چه سیستمی دستورها را تست کرده‌ای (ویندوز/WSL2، لینوکس، مک)
- اگر کامپوننت تازه‌ای ساخته‌ای، چرا لازم بود
- یک عکس صفحه در دارک‌مود و یکی در لایت‌مود، اگر چیزی در ظاهر عوض کرده‌ای

---

## سؤال داری؟

<abbas.mossavi1378@gmail.com>

اگر مطمئن نیستی فصلی که در نظر داری با روح این مجموعه جور است یا نه،
قبل از نوشتن یک issue باز کن و بپرس. بهتر از این است که ده ساعت بنویسی و
بعد معلوم شود جایش اینجا نبود.
