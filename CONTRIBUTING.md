<div align="right">

# راهنمای مشارکت در کدنامه

خوش آمدی. این سند می‌گوید چطور یک **فصل** بنویسی، خطایی را اصلاح کنی یا
سایت را بهتر کنی. همین راهنما روی خود سایت هم هست: `/contributing`.

</div>

---

## سه راه مشارکت

1. **نوشتن فصل** — سنگین‌ترین و ارزشمندترین کار.
2. **اصلاح خطا** — دستوری که دیگر کار نمی‌کند، خروجی‌ای که عوض شده، جمله‌ای که مبهم است.
3. **بهبود سایت** — دسترس‌پذیری، سرعت، ترجمهٔ انگلیسی، طراحی.

---

## راه‌اندازی

```bash
git clone https://github.com/SeyedAb2/code-name.git
cd code-name
npm install
npm run dev        # http://localhost:3000
```

---

## یک فصل چه شکلی است

هر فصل یک فایل HTML است: `content/<مسیر>/<فصل>.html`. بالای فایل یک سربرگ
کوتاه می‌آید و بعد از آن بخش‌ها. هر بخش یک `<section>` با `id` است که خودش
در فهرست مطالب و در محاسبهٔ پیشرفت می‌نشیند.

```html
<!--meta
title: عنوان فصل
desc:  یک جملهٔ کوتاه برای موتور جستجو
-->

<section id="why">
  <h2><span lang="fa">چرا این کار</span><span lang="en">Why this</span></h2>
  <p lang="fa">متن فارسی…</p>
  <p lang="en">English text…</p>
</section>
```

متن فارسی و انگلیسی کنار هم می‌آیند و سوییچ زبان یکی را پنهان می‌کند؛ پس هر
جمله باید هر دو نسخه را داشته باشد.

بعد از ساختن فایل، در `data/courses.js` پرچم `ready` همان فصل را روشن کن و
یک بار `npm run manifest` بزن. صفحهٔ فصل خودش ساخته می‌شود.

### کلاس‌های آماده

| کلاس | برای چه |
|---|---|
| `.term` / `.code` | جعبهٔ ترمینال و فایل کد، با دکمهٔ کپی |
| `.note` (`safe` / `warn` / `danger`) | یادداشت |
| `.cards` / `.card` | چند مفهوم کنار هم |
| `.diagram` | نمودار SVG با رنگ‌های سازگار با تم |
| `.ex` | تمرین، با `details.ex-hint` و `details.ex-sol` |
| `.pitfalls` / `.pit` | دام‌ها، با پیام خطای دقیق در `.pit-e` |
| `.cheat` | مرجع سریع پایان فصل |
| `.capsule` | پروژهٔ پایان فصل |

رنگ‌بندی کد با دست انجام می‌شود: `.k` دستور، `.f` سوئیچ، `.c` توضیح،
`.s` خروجی، `.w` خطا، `.p` مسیر و `.d` پرامپت (که کپی نمی‌شود).

---

## معیار کیفیت

یک فصل وقتی آماده است که همهٔ این‌ها را داشته باشد:

- [ ] دست‌کم ۴۵۰۰ کلمهٔ فارسی، با نسخهٔ انگلیسی کنارش
- [ ] دست‌کم ۱۸ تمرین، از ساده به سخت، هر کدام با راهنمایی، پاسخ کامل و **خروجی مورد انتظار**
- [ ] دست‌کم ۳۰ بلوک کد یا ترمینال با خروجی واقعی
- [ ] دست‌کم دو نمودار و یک جدول مقایسه
- [ ] دست‌کم چهار دام، هر کدام با پیام خطای دقیق و راه‌حل
- [ ] یک مرجع سریع و یک پروژهٔ پایان فصل
- [ ] **هر دستور قبل از انتشار اجرا شده باشد** — خروجی حدسی ممنوع

قاعدهٔ آخر مهم‌ترین است. اگر نمی‌توانی دستوری را اجرا کنی، ننویسش.

---

## زبان و لحن

گفت‌وگویی ولی حرفه‌ای؛ مثل مهندسی که کنارت نشسته و توضیح می‌دهد. برای هر
چیزی **چرا**یش را بگو. اصطلاح فنی را به انگلیسی نگه دار و در اولین برخورد
معنی‌اش را بنویس.

---

## کامیت و Pull Request

- هر PR یک موضوع.
- پیام کامیت **انگلیسی** و کوتاه.
- اگر فصل نوشته‌ای، در توضیح PR بگو کدام دستورها را روی چه سیستم و چه
  نسخه‌ای اجرا کرده‌ای.
- قبل از فرستادن: `npm run build` باید بدون خطا تمام شود.

---

## تماس

سیدعباس موسوی اصل — [github.com/SeyedAb2](https://github.com/SeyedAb2) ·
[abbas.mossavi1378@gmail.com](mailto:abbas.mossavi1378@gmail.com)
